# TODO
- [ ] redesign activity card to make sure that status is not on it's own row and number of spots left only shows at bookings with max_attendees. Also make sure the card uses the scout-card component. maybe rethink placement of info so that time and place comes at same row to reduce vertical real estate.
- [ ] Change buttons on activity detail page to only have change booking which in turn open modal where you can delete booking.
- [ ] Make sure booking form is split from activity details (also shown clearly on long descriptions)
- [ ] modem is not trigggered properly for route changes (back button in wwrapper app is not working due to this probably)
- [ ] Add outlined or icon only button around all heart buttons.
- [ ] Refactor so that bookings and favourites are fetched in one api call which returns only valid states (Booked(Booking), Favourited, None)
- [ ] Add white background behind filters
- [ ] Add search icon in search bar